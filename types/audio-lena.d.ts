export = index;
declare function index (options: any): any;
declare namespace index {
  const channels: number;
  const duration: number;
  const flac: ArrayBuffer;
  const flacBase64: string;
  const flacDatauri: string;
  const mp3: ArrayBuffer;
  const mp3Base64: string;
  const mp3Datauri: string;
  const numberOfChannels: number;
  const ogg: ArrayBuffer;
  const oggBase64: string;
  const oggDatauri: string;
  const raw: ArrayBuffer;
  const rawBase64: string;
  const rawDatauri: string;
  const sampleRate: number;
  const samplesCount: number;
  const wav: ArrayBuffer;
  const wavBase64: string;
  const wavDatauri: string;
}
